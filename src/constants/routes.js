
const ROUTE_PREFIX = {
  API: "/api/v1",
  USERS: "/users",
};

const USER_ROUTES = {
  SIGNUP: "/signup",
  LOGIN: "/login",
  PROFILE: "/profile",
};

module.exports = {
  ROUTE_PREFIX,
  USER_ROUTES,
};
